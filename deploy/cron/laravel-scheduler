# Crontab do usuário www-data (ou deploy) — agenda Laravel
# Instalar: sudo crontab -u www-data -e
# Colar a linha abaixo (ajustar caminho):

* * * * * cd /var/www/linha-leve && php artisan schedule:run >> /dev/null 2>&1
